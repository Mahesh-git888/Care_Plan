import { LandingPage } from "@/components/landing-page";
import { verticals } from "@/data/verticals";

export default function DementiaPage() {
  return <LandingPage vertical={verticals.dementia} />;
}
