.next
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.DS_Store
*.tsbuildinfo

# Local env — never commit secrets
.env
.env.local
.env.new
.env.*.local

# Local lead store (when PORTEA_LEADS_FILE points inside the repo)
portea-leads.jsonl

.vercel
.env*.local
